#include <stdio.h>
int main ()
//for if statement
{
    int x=30,y=45;
   
    if(x<y)
    {
        printf("variable x is less than y");
    }
    return 0;
}
//Explanation: the condional (x<y) specified in the "if" return true for the value of x and y
// so the statement inside the body of if is executed. 